proc test-algo(A, B)
    for j <- 2 to A.length do
	// Insert $A[j]$ into $A[1 .. j-1]$
        key <- A[j] 
        i <- j - 1
        while i > 0 and A[i] > key do
            A[i+1] <- A[i]
            i <- i - 1
        end
        A[i+1] <- key
    end
    if x == 3 then do
        {{Do nothing}}
    end
end
